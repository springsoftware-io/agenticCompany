# Reusable Workflow Configuration
# This workflow defines common variables and can be called by other workflows
# Usage: Include this at the top of your workflow with anchors and aliases

name: Workflow Configuration (Reference Only)

# This workflow is never triggered - it's a reference document
on:
  workflow_dispatch:
    inputs:
      do_not_run:
        description: 'This is a reference file only - do not run'
        required: false

# Environment variables available to all workflows
env:
  # Core paths
  CORE_DIR: seedgpt-core
  SRC_DIR: seedgpt-core/src
  TESTS_DIR: seedgpt-core/tests
  SCRIPTS_DIR: seedgpt-core/scripts
  
  # Key files
  REQUIREMENTS_FILE: seedgpt-core/src/requirements.txt
  TESTS_REQUIREMENTS_FILE: seedgpt-core/tests/requirements.txt
  
  # Agent paths
  CLAUDE_AGENT_DIR: seedgpt-core/src/claude-agent
  GEMINI_AGENT_DIR: seedgpt-core/src/gemini-agent
  AGENTS_DIR: seedgpt-core/src/agents
  
  # Test paths
  TESTS_UNIT_DIR: seedgpt-core/tests/unit
  TESTS_INTEGRATION_DIR: seedgpt-core/tests/integration
  TESTS_CACHE_DIR: seedgpt-core/tests/.pytest_cache
  COVERAGE_DIR: seedgpt-core/tests/htmlcov
  COVERAGE_XML: seedgpt-core/tests/htmlcov/coverage.xml
  
  # Script paths
  CLAUDE_SCRIPTS_DIR: seedgpt-core/src/claude-agent/scripts
  GEMINI_SCRIPTS_DIR: seedgpt-core/src/gemini-agent/scripts

# YAML anchors for reusable path patterns
# Note: These are for documentation - GitHub Actions doesn't support anchors across files
# Copy these patterns into your workflows as needed

# Path patterns for triggers:
# paths:
#   - 'seedgpt-core/src/gemini-agent/**'
#   - 'seedgpt-core/src/claude-agent/**'
#   - 'seedgpt-core/src/agents/**'
#   - 'seedgpt-core/tests/**'
#   - 'seedgpt-core/Makefile*'
#   - '.github/workflows/**'

jobs:
  reference-only:
    runs-on: ubuntu-latest
    steps:
      - name: This is a reference file
        run: |
          echo "⚠️ This workflow is for documentation purposes only"
          echo "📖 See .github/workflows/README-CONFIG.md for usage"
          echo "❌ Do not run this workflow"
          exit 1
